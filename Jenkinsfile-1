pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh 'git --version'
                sh 'docker --version'
                sh 'docker run node_tester:v2'
            }
        }
    }
}