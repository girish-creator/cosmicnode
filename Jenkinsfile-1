pipeline {
  agent {
    docker { image 'cosmicnode/node-tester:v2' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'ls -l'
        sh 'docker run cosmicnode/node-tester:v2'
      }
    }
  }
}